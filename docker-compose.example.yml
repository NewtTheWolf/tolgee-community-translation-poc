services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ct
      POSTGRES_PASSWORD: ct
      POSTGRES_DB: ct
    ports: ['5432:5432']
    volumes: ['ct-pgdata:/var/lib/postgresql/data']
volumes:
  ct-pgdata:
